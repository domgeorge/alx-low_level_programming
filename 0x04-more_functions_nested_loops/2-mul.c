#include "main.h"

/**
 * mul - performs the multiplication of two integers a and b
 * @a: integer to be multiplied with b
 * @b: integer to be multiplied with a
 * Return: outputs the result or the operation
 */

int mul(int a, int b)
{
	return (a * b);
}
