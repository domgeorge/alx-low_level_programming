#include "main.h"
#include <string.h>

/**
 * _strlen - gives an output of the string length
 * @s: points to the integer
 * Return: returns the string length
 */

int _strlen(char *s)
{
	return (strlen(s));
}
